{% set _ssl_on = (item.ssl_key_is_on_destination is defined and item.ssl_key_is_on_destination) or (item.ssl_server_cert is defined and item.ssl_server_cert) or (item.ssl_self_signed is defined and item.ssl_self_signed) %}
{% if _ssl_on %}
# Redirect plain HTTP to HTTPS for {{ item.server_name }}.
server {
    listen 80;
    server_name  {{ item.server_name }};
    return 301 https://$host$request_uri;
}
{% endif %}
server {
        server_name  {{ item.server_name }};
        keepalive_timeout 60;
        client_max_body_size 10M;
        more_clear_headers Server X-Powered-By;
        location / {
            root {{ item.folder }};
            autoindex on;
        }

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        # SSL_CERTIFICATE_ANSIBLE
{% if _ssl_on %}
# BEGIN SSL Parameters by Ansible for {{ item.server_name }}
# SSL parameters
listen 443 ssl;
ssl_certificate /etc/ssl/custom_ssl/{{ item.certificate_name | default(item.server_name, true) }}/server.crt;
ssl_certificate_key /etc/ssl/custom_ssl/{{ item.certificate_name | default(item.server_name, true) }}/server.key;
ssl_session_timeout 30m;
ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
ssl_ciphers 'ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-AES256-GCM-SHA384:DHE-RSA-AES128-GCM-SHA256:DHE-DSS-AES128-GCM-SHA256:kEDH+AESGCM:ECDHE-RSA-AES128-SHA256:ECDHE-ECDSA-AES128-SHA256:ECDHE-RSA-AES128-SHA:ECDHE-ECDSA-AES128-SHA:ECDHE-RSA-AES256-SHA384:ECDHE-ECDSA-AES256-SHA384:ECDHE-RSA-AES256-SHA:ECDHE-ECDSA-AES256-SHA:DHE-RSA-AES128-SHA256:DHE-RSA-AES128-SHA:DHE-DSS-AES128-SHA256:DHE-RSA-AES256-SHA256:DHE-DSS-AES256-SHA:DHE-RSA-AES256-SHA:AES128-GCM-SHA256:AES256-GCM-SHA384:AES128-SHA256:AES256-SHA256:AES128-SHA:AES256-SHA:AES:CAMELLIA:DES-CBC3-SHA:!aNULL:!eNULL:!EXPORT:!DES:!RC4:!MD5:!PSK:!aECDH:!EDH-DSS-DES-CBC3-SHA:!EDH-RSA-DES-CBC3-SHA:!KRB5-DES-CBC3-SHA';
ssl_prefer_server_ciphers on;
# END SSL Parameters by Ansible for {{ item.server_name }}
{% endif %}

}
