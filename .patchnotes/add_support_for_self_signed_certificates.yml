type: feature
description: "Router: self-signed TLS vhosts (ssl_self_signed) for protected LANs where Let's Encrypt/ACME cannot verify; generate a self-signed cert on the host, listen on 443 plus an 80->https redirect. Also fixes the accme-challenge typo in the ACME location."
breaking: false
