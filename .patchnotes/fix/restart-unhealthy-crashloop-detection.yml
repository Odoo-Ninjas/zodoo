type: fix
description: "restart_unhealthy_containers: also detect crash-looping containers (via RestartCount episode tracking across cron ticks), stuck-in-'starting' containers and crashed 'exited' containers (non-clean exit code / OOM-killed), not only health=unhealthy"
breaking: false
